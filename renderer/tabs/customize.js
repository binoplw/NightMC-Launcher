// Перетаскивание элементов (будет доработано позже)
const customizeMode = localStorage.getItem('nightmc_customize_mode') === 'true';
// Заглушка для будущей реализации
window.log('Customize module loaded');